apiVersion: v1
kind: Service
metadata:
  name: my-single-pod-service
  labels:
    env: prod
    owner: SergeiChes
spec:
  selector:
    project: kgb #Selectiing PODS with those lLabels!!!
  type: LoadBalancer
  ports:
  - name: app-listener
    protocol: TCP
    port: 80 # Port on Load Balancer
    targetPort: 80 # Port on POD

